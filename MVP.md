# 大模型在机甲应用场景中的 MVP 计划

> 验证大模型在机甲上的实时交互形式。  
> 当前 Demo 以台球场景为例，验证「场景识别 - 状态判断 - 交互提示 - 结果复盘」的完整闭环。

## 1. MVP 目标

本 MVP 主要验证大模型在机甲应用场景中的交互可行性，重点不是完成完整的台球教学系统，而是通过一个相对明确、可控的台球场景，验证以下能力：

1. 佩戴眼镜后，能够识别当前运动场景（台球）。
2. 能够根据实际画面判断用户所处状态：观察、瞄准、击打、复盘。
3. 在观察状态下，给出推荐的目标球和目标袋。
4. 在瞄准状态下，判断手势/姿势是否正确，并辅助判断目标球落袋方向。
5. 在完成击打后，判断击打结果，并给出简要复盘。

## 2. Demo 需求

### 2.1 核心需求

- 告知用户应该击打哪个球。
- 告知用户目标球应该进入哪个袋。
- 判断目标球是否进袋。
- 纠正用户左手姿势。

### 2.2 交互触发条件

- 语音触发。
- 场景状态触发（首选）。
  - 当系统识别到状态变化时，自动输出对应提示。
  - 例如从观察状态进入瞄准状态时，开始进行姿势判断。

## 3. 场景状态设计

系统根据眼镜视频流中的画面变化，将用户当前行为划分为以下状态：

### 3.1 观察状态

用户正在观察台面，尚未进入击球动作。

系统输出：

- 推荐目标球。
- 推荐目标袋。
- 简单说明推荐原因。

### 3.2 瞄准状态

用户开始准备击球，画面中出现手、球杆或明显瞄准动作。

系统输出：

- 判断瞄准方向是否合理。
- 判断左手姿势是否存在明显问题。
- 提示用户调整姿势或方向。

### 3.3 击打状态

用户完成击球动作，画面中出现球体运动或台面状态变化。

系统输出：

- 击打过程中尽量减少提示，避免干扰用户。
- 记录击打前后的台面状态，用于后续判断。

### 3.4 复盘状态

击打完成后，球体运动停止，系统对结果进行判断。

系统输出：

- 判断是否进球。
- 判断击打结果是否符合预期。
- 给出简要复盘。

## 4. YOLO 视觉识别模块

YOLO 主要负责从眼镜视频流中识别台球场景中的关键对象，并为状态判断和交互提示提供结构化信息。

### 4.1 判断目标球和目标袋

通过 YOLO 识别视野内的球和袋口，并基于画面位置关系判断推荐目标球和目标袋。

当前简化策略：

- 识别画面中的球体。
- 识别画面中的袋口。
- 优先选择与母球距离较近、且更容易形成击打路径的球作为目标球。
- 根据目标球与袋口的位置关系，推荐目标袋。

### 4.2 判断是否进球

通过 YOLO 对击打前后画面中的球数量和位置变化进行判断。

判断依据：

- 击打前后球数量是否减少。
- 目标球是否从画面中消失。
- 目标球是否靠近或进入袋口区域。

### 4.3 判断左手姿势

通过 YOLO 或 VLM 判断用户左手姿势是否属于正确姿势。

当前重点先判断明显错误姿势，例如：

- 左手位置明显偏离球杆支撑区域。
- 左手不稳定。
- 左手遮挡目标球或母球。

### 4.4 标注数据和格式

| 标注对象 | 标注类型 | 标注内容 | 用途 |
| --- | --- | --- | --- |
| 球 | 目标框 | 数量、位置、与母球/袋口的距离关系 | 1. 判断是否进球 2. 判断推荐击球 |
| 袋口 | 目标框 | 位置、与目标球的距离关系 | 1. 判断目标进球袋 |
| 手 | 目标框 | 位置、姿势状态 | 1. 判断姿势是否错误 |
| 球杆 | 目标框 | 位置、方向 | 1. 判断瞄准状态 2. 辅助判断击打动作 |

## 5. Agent / VLM 模块

### 5.1 实现形式

采用“大模型调用小模型”的形式实现：

- 小模型负责视觉检测、状态判断和结构化结果输出。
- 大模型作为 Agent，根据小模型输出的结果生成自然语言提示。
- YOLO、姿势检测、进球判断等能力可以封装成 Tool，由 Agent 按需调用。

### 5.2 Agent 输入

Agent 接收视频流判断状态进行调用小模型输出结构化信息。

Agent 接收视觉模块输出的结构化信息，例如：

```json
{
  "state": "aim",
  "target_ball": "ball_3",
  "target_pocket": "right_top",
  "left_hand_status": "unstable",
  "shot_result": null
}
```

### 5.3 Agent 输出

Agent 根据当前状态输出简短、可执行的提示，例如：

- 观察状态："建议先打右侧这颗球，目标袋是右上袋。"
- 瞄准状态："左手支撑不太稳定，先稳住再击球。"
- 复盘状态："这次没有进球，方向略微偏左。"

## 6. Runtime 设计

Runtime 负责连接输入、模型处理和输出展示，是 Demo 能否实时运行的关键。

### 6.1 Runtime 能力

- 可以流式接收眼镜视频流。
- 可以对视频流进行实时处理并输出结果。
- 可以输出语音提示或问题显示。
- 支持替换不同的视觉 backbone。
- 支持接入不同的大模型或 VLM。

### 6.2 Runtime 流程

```text
眼镜视频流
  ↓
视频帧采样
  ↓
YOLO / VLM 视觉识别
  ↓
场景状态判断
  ↓
Agent 生成提示
  ↓
语音 / 文本 / 画面提示输出
```

## 7. Demo 结果预期

最终希望在台球场景下实现一个实时交互 Demo，具备以下能力：

- 能够识别用户所处的台球场景。
- 能够判断用户当前处于观察、瞄准、击打或复盘状态。
- 能够在观察状态下给出击球建议。
- 能够在瞄准状态下进行简单姿势纠正。
- 能够在击打完成后判断结果并进行复盘。
